
class APIRequest {

    let method:String
    let path:String
    let params: [String: Any]

    init(path: String, method: String, params: [String: Any]) {
        self.path = path
        self.method = method
        self.params = params
    }
}

struct API {

    {% for tag in tags %}
    struct {{ tag.name | upperCamelCase }} {}
    {% endfor %}

}
